<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/objectsColor</item>
        <item name="colorPrimaryDark">@color/objectsColor</item>
        <item name="colorAccent">@color/objectsColor</item>
        <item name="android:titleTextStyle">@style/MyTextAppearance</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorControlActivated">@color/white</item>
    </style>

    <!-- стиль для текста -->
    <style name="MyTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="recButton" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/objectsColor</item>
        <item name="colorControlHighlight">@color/objectsColor</item>
    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/objectsColor</item>
    </style>


</resources>
